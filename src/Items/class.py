class  case

    taille = 16

    ofessety = 0
    for j in variable:
        ofessetx = 0
        for i in variable:
            dessiner(x, y, index x + ofessetx, index y + ofessety, 16, 16)


            break 
            ofesset + taille
        ofesset = 0
        ofessety + taille

    ofessety = 0
    for j in variable:
        ofessetx = 0
        for i in variable:
            position (x,y)

            ofesset + taille
        ofesset = 0
        ofessety + taille
